name: Sync to marketplace

on:
  release:
    types: [published]

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout plugin repo
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6
        with:
          path: plugin

      - name: Checkout marketplace repo
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6
        with:
          repository: bouob/claude-plugins
          token: ${{ secrets.MARKETPLACE_TOKEN }}
          path: marketplace

      - name: Sync plugin files
        run: |
          PLUGIN_DIR="marketplace/plugins/repo-vet"
          rm -rf "$PLUGIN_DIR"
          mkdir -p "$PLUGIN_DIR"

          cd plugin
          find . \
            -not -path './.git/*' -not -path './.git' \
            -not -path './.github/*' -not -path './.github' \
            -not -path './node_modules/*' -not -path './node_modules' \
            -not -path './desc-opt/*' -not -path './desc-opt' \
            -not -path './__pycache__/*' -not -path './__pycache__' \
            -not -path './.vscode/*' -not -path './.vscode' \
            -not -path './.idea/*' -not -path './.idea' \
            -not -name '.gitignore' \
            -not -name '.release-please-manifest.json' \
            -not -name 'release-please-config.json' \
            -not -name 'CHANGELOG.md' \
            -not -name '*.log' \
            -not -name '.env' \
            -not -name '.env.*' \
            | while read -r item; do
                if [ -d "$item" ]; then
                  mkdir -p "../$PLUGIN_DIR/$item"
                elif [ -f "$item" ]; then
                  cp "$item" "../$PLUGIN_DIR/$item"
                fi
              done

      - name: Create PR and auto-merge
        working-directory: marketplace
        env:
          GH_TOKEN: ${{ secrets.MARKETPLACE_TOKEN }}
          TAG: ${{ github.event.release.tag_name }}
        run: |
          BRANCH="sync/repo-vet-${TAG}"

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b "$BRANCH"
          git add -A
          git diff --cached --quiet && echo "No changes to sync" && exit 0
          git commit -m "sync: repo-vet ${TAG}"
          git push origin "$BRANCH"
          PR_URL=$(gh pr create --title "sync: repo-vet ${TAG}" --body "Automated sync from repo-vet ${TAG}.")
          gh pr merge "$PR_URL" --squash
